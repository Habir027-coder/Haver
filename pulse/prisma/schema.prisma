generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  // For production replace with Postgres url: provider = "postgresql" and set DATABASE_URL
  url      = env("DATABASE_URL", "file:./dev.db")
}

model User {
  id           Int      @id @default(autoincrement())
  name         String
  email        String?  @unique
  role         String   @default("employee")
  pinHash      String?
  passwordHash String?
  createdAt    DateTime @default(now())
  punches      Punch[]
}

model Punch {
  id        Int      @id @default(autoincrement())
  user      User     @relation(fields: [userId], references: [id])
  userId    Int
  type      String   @default("Ponto")
  timestamp DateTime @default(now())
}
